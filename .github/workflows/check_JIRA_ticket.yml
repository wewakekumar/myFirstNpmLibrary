name: Check JIRA

on:
  pull_request:
    types:
      - opened
      - reopened
      - edited
      - synchronize
      - ready_for_review
      - labeled

jobs:
  detect:
    name: Detect JIRA ticket
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false
    steps:
      - name: Detect JIRA
        id: detect
        uses: actions/github-script@v7
        env:
          JIRA_PAT: ${{ secrets.JIRA_PAT }}
        with:
          script: |
            const prDescription = context.payload.pull_request.body || "";

            console.log("#process.env", process.env.JIRA_PAT, "sjhjks");
            
            fetch('https://vivekrajwe1.atlassian.net/rest/api/2/issue/CRM-3', {
              method: 'GET',
              headers: {
                'Authorization': `Basic ${Buffer.from('vivekrajwe1@gmail.com:ATATT3xFfGF0mAUpA_rz0ATKEpxsyeVXhA-dOljGTdBdiaXxqKQOXpTsTgw7vxskLUUV1yHlWXbPQZRRzQEspMnGQOe5W6Qk--MvD2MSBOTav3A0pyAMS_xssFgC2MQQPneZdf5zJw_OeTZa_Mj4cymiKL7BAupJ8KYzOgG0EO8G__V1tsQyvZg=1B3E1309').toString('base64')}`,
                'Accept': 'application/json'
              }
            })
              .then(response => {
                console.log(
                  `Response: ${response.status} ${response.statusText}`
                );
                return response.text();
              })
              .then(text => {
                const jsonText = JSON.parse(text);
                const assignee = jsonText.fields.assignee.emailAddress;
                console.log("#assignee", assignee);
              })
              .catch(err => console.error("#error", err));
